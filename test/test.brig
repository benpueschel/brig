struct Vec2 {
	x: u32,
	y: u32,
}

extern fn vec2(x: u32, y: u32): Vec2 {
	return struct Vec2 { x: x, y: y };
}

extern fn max(a: u32, b: u32): u32 {
	return if a >= b {
		a
	} else {
		b
	};
}

extern fn maxten(a: u32): u32 {
	let ten: u32 = 10;
	if a != ten {
		return max(a, ten);
	}
	return ten;
}
