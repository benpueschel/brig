struct Vec2 {
	x: u32,
	y: u32,
}

struct Vec4 {
	a: Vec2,
	b: Vec2,
}

extern fn vec2(x: u32, y: u32): Vec2 {
	return struct Vec2 { x: x, y: y };
}

extern fn vec4(x: u32, y: u32, z: u32, w: u32): Vec4 {
	return struct Vec4 { a: vec2(x, y), b: vec2(z, w) };
}
