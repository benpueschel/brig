extern fn bar(x: u32): u32;

extern fn ayo(a: u32, b: u32, c: u32, d: u32, e: u32, f: u32, g: u32, h: u32): u32 {
    return a + b + c + d + e + f + g + h;
}

extern fn foo(x: u32): u32 {
    return bar(x - 5);
}

extern fn max(a: u32, b: u32): u32 {
	return if a > b {
		a
	} else {
		b
	};
}
